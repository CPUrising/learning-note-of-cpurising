#include <iostream>#include <string>#include <vector>#include <algorithm>#include <iterator>  //required by iostream iteratorsusing namespace std;int main(){     istream_iterator<string> is(cin);     istream_iterator<string> eof;     vector<string> text;     /*copy 是算法库中的函数，功能是将[is, eof) 范围内的数据复制到目标位置。     back_inserter(text) 是一个插入迭代器，它会自动调用 text.push_back()，将读取到的字符串逐个添加到 vector 末尾。         效果：从键盘输入的所有字符串（以空格或换行分隔）会被依次存入 text 容器中。*/     copy(is, eof, back_inserter(text));     sort(text.begin(), text.end());     ostream_iterator<string> os(cout, "   ");     copy(text.begin(), text.end(), os);}